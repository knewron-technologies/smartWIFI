-- this is system configuration and variables' file
-- !! do not edit if you don't know what you're doing !!
-- if any variable value is unknown, remove corresponding line
-- to un-declare any variable set it to nil

-- below system variables are available for access throughout the
-- power cycle / session
deviceID = wifi.sta.gethostname();
apSSID = "Knewron";
apPassword = "Knewron@123#";
hotspotSSID = "smartWIFI";
hotspotPassword = "smartwifi";
consoleOutput = true;
consoleLog = true;

-- below system variables are used only during startup and then
-- are set to nil after use
autoUpdate = true;
autoConnect = true;
uartSpeed = 9600;
rgbType = "CC";
